// FromJSON 节点插件（type 59）：JSON 字符串 → 对象（解析失败降级原文）。
// 前端无特殊渲染（通用 in/out 端口）；输出结构可用 EdFW.ioTable 编辑（输出字段 → output.object 的 schema）。
EdFW.register({
  type: "59", label: "FromJSON", icon: "{←}", category: "data",
});
