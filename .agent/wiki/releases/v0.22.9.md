# v0.22.9 发布记录

> 发布：2026-09-02，Git 提交 `c591501`，PyPI 包 `agt-agent` 已上线（`src/__init__.py` 版本 0.22.8 → 0.22.9，edit 工具直接改）。自 0.22.8 的 **3 笔实质提交**：①Provider 一句话简介（选择理由，`620fd3d`）；②工具箱卡片简介 Tool.brief 三级优先（`620fd3d`，与①同 commit——2026-09-02 功能批，功能细节见 home.md 快速事实十七）；③README 中文标语置顶 + 内置 Provider 目录表 + OrcaRouter referral 链接（`84097f7` + `58b046e`，本版新增落点）。0.14.1 → 0.22.9 共推 **42 个版本**。`pip install -U agt-agent` + `/restart` 一次本 session 切到 0.22.9 全量形态。

## 发布内容总览

| 类别 | 内容 | 详述 |
|---|---|---|
| 🧭 **Provider 一句话简介（选择理由）** | models.preset.json 五家 provider 增 `icon`（emoji）+ `brief`（一句话理由）；三处消费——onboarding 弹窗头部横幅（大 icon + brief + desc 小字）、模型下拉 optgroup 分组名 `✨ icon prov — brief`（option title 悬停）、README「配置模型」目录表 | [config-and-models · provider 一句话简介](../guides/config-and-models.md#provider-一句话简介选择理由preset-增-iconbriefonboarding下拉readme-三处消费2026-09-02commit-620fd3d) |
| 🧰 **工具箱卡片简介补齐** | 新 `src/tool_briefs.py`（TOOL_BRIEFS 集中字典 130+ 条）+ Tool 类 `brief` 参数——三级优先 `显式 brief > 字典 > description 首句兜底`；修掉前端浮窗 `.tpk-desc` 自 2026-09-01 建浮窗起从未有数据的隐形缺口 | [tool-form · Tool.brief 三级优先](../features/tool-form.md#工具卡片简介补齐toolbrief-三级优先--tool_briefspy-集中字典2026-09-02commit-620fd3d) |
| 📄 **README 中文标语 + Provider 目录表 + referral** | README 中文标语置顶（`84097f7`）+ 内置 Provider 目录表（与 preset 五家同步）+ OrcaRouter 申请页换 referral 链接（`84097f7` 换、`58b046e` 再 update——**×3 修正闭环**） | 仓库根 README.md（无独立 wiki 页） |

## 发布细节

- **620fd3d 功能批随版落地**：①/② 两笔实质功能（provider 一句话简介 + Tool.brief 三级优先）同为 commit `620fd3d`——2026-09-02 功能批的一部分，功能细节已录入 home.md 快速事实十七与 [config-and-models](../guides/config-and-models.md)、[tool-form](../features/tool-form.md) 对应章节；本版将其打包发布，另两台实例（8000 / 团队成员们）升级后 `pip install -U agt-agent` 即拿到带简介的 preset——用户在模型下拉里选 OrcaRouter 时即从 referral 链接申请 key。
- **README 三件（`84097f7` + `58b046e`）**：中文标语置顶（宣传位第一屏）；内置 Provider 目录表随 preset 五家（modelscope 🎁 / z.ai ⚡ / deepseek-official 🐋 / siliconflow 🌊 / orcarouter 🧭）；OrcaRouter 申请页 referral 链接 84097f7 换新、58b046e 再 update——git log 可见「×3 修正」全过程，发布前 git status 已干净（README.md / models.preset.json 改动全部入库）。
- **版本 bump**：edit 工具直接改 `src/__init__.py` 的 `__version__`（0.22.8 → 0.22.9）——0.22.4 确立的「脚本直改版本号」发版流程延续，干净一次过。
- **发布链验证**：wheel 内容抽查——`tool_briefs.py`（10.6KB）/ `models.preset.json`（五家 brief 全在）/ `index.html`（onboarding 横幅 + optgroup 简介两处代码）全部进包 ✅；PyPI 索引已刷新 `latest: 0.22.9`（上传后 ~30s 延迟，符合常态）。

## 版本演进

| 版本 | 关键交付 |
|------|---------|
| **0.22.9** | Provider 一句话简介（选择理由）+ 工具箱卡片 Tool.brief 三级优先（620fd3d 功能批发布落地）+ README 中文标语置顶 / Provider 目录表 / OrcaRouter referral 链接三件 |
| 0.22.8 | 中间版本（无独立发布页）——2026-09-02 功能批的发布载体（night_tasks 五件套 / 团队管理 / assembly 段形态简化 / steps 双模 / func 扩容 / registry 防覆盖 / cache_breakpoint / 连接补发 current_turn 等，内容散见 [home.md 快速事实增补 2026-09-02](../home.md)） |
| 0.22.7 | 投影引擎三区重构（tail 六子段拆段 + 区3 统一包裹 merge 末条 + 钩子注入 merge 化 + hook_note 落盘/前端折叠）——消息形状由对话本体决定、前缀缓存 byte-stable 打满（见 [v0.22.7](v0.22.7.md)） |
| 0.22.5 | 缓存经济学集中交付（fc 持久化 + 触发判定两修）+ recent-file 100K 收口 + 配置隔离 + 可观测性四件（见 [v0.22.5](v0.22.5.md)） |
| 0.22.4 | service_exit 唤醒策略化 + bg_task 完成恒通知（见 [v0.22.4](v0.22.4.md)） |
| 0.22.3 | per-provider 缓存经济学双参数 + DeepSeek 缓存实证收口 + recent-file v3 终版（见 [v0.22.3](v0.22.3.md)） |

> 自 0.14.1 → 0.22.9 共推 **42 个版本**，全程 Agt 自我迭代（builds itself）。本版是 2026-09-02「一句话简介」功能批（provider 选择理由 + 工具卡片简介双落地，commit 620fd3d）的发布收口，外加 README 宣传位三件（中文标语置顶 + Provider 目录表 + referral 链接）：代码侧给「人挑 provider/工具」补上了理由，README 侧给「新用户进仓库」补上了第一屏——社区/团队推广向的发布。

## 相关页面

- [config-and-models · provider 一句话简介](../guides/config-and-models.md#provider-一句话简介选择理由preset-增-iconbriefonboarding下拉readme-三处消费2026-09-02commit-620fd3d) — 本批核心①，preset icon+brief 三处消费
- [tool-form · Tool.brief 三级优先](../features/tool-form.md#工具卡片简介补齐toolbrief-三级优先--tool_briefspy-集中字典2026-09-02commit-620fd3d) — 本批核心②，130+ 条集中字典 + 三级优先
- [知识库导航 · 快速事实十七](../home.md) — 620fd3d 功能批的原始记录（2026-09-02）
- [v0.22.7 发布记录](v0.22.7.md) — 前一有独立页版本（投影引擎三区重构）
- [知识库导航](../home.md) — 版本状态总览
