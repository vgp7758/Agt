# v0.29.4 发布记录

> 发布：2026-09-18（用户「发个补丁吧」），发布提交 `3dd9a7f`，tag `v0.29.4`，PyPI 版本 0.29.4（`src/paths.py` VERSION 0.29.3 → 0.29.4）。**scheduler 专题补丁版**——自 v0.29.3 以来 3 个实质提交全部来自定时/到点任务调度子系统（src/background.py + background_tools.py）。

## 发布内容总览

| 类别 | 内容 | commit |
|---|---|---|
| ⏰ **组合模式** | `every_seconds + at` 同给 = at 相位起步、之后每 N 秒循环（相位栅格不漂移） | `9a88107` |
| 💾 **定时任务持久化** | 落 `session.extra_state["schedules"]`（随 meta.json），任务跨重启存活 | `95649e3` |
| 🐞 **同名覆盖摘旧** | 同名任务再设先摘旧 `_schedules` 条目——重复投递根因修复（pre_post 双投实锤） | `8ed09c6` |

三件机制详述均见 [定时/到点任务调度](../features/background-scheduler.md)（组合模式 / 持久化 / 同名摘旧三节）。

## 要点速记

- **组合模式**：`add_schedule('巡检', every_seconds=300, at='2026-09-18T10:00:00', message=...)`——at 为首触发相位起点；at 已过去自动对齐下一相位点（10:00 起每 5min、现在 11:43 → 首发对齐 11:45，不走「now+5min」漂移算法）；短格式 `HH:MM` 与相位语义冲突，明确报错
- **持久化**：存任务定义 + **`at_origin` 相位锚**（at 未填默认记 now——「假装第一次已在 now 触发过」），**不存 `next_fire`**——恢复时按锚点重算（与组合模式同一套相位对齐算法，重启不漂移）；顺手 `Lock→RLock`（嵌套加锁）+ `_LOG` 补定义
- **同名摘旧**：三处注册段（`add_interval` / `add_interval_at` / `add_at`）统一先 pop 旧条目再注册——同名改时间重设不再双投

## 更新方式

```bash
/restart                         # 本机（editable 安装）即生效；重启后持久化任务自动恢复
pip install -U agt-agent         # 其它实例 → 0.29.4
```

本机 `/restart` 一次即把本批（连同 v0.29.3 未生效的多实例通信 / MCP 两级化）全部装载——之后设置的定时任务（组合模式 / 每日闹钟 / interval 循环）**跨重启存活**，同名再设不再双投。

## 版本演进

| 版本 | 关键交付 |
|------|---------|
| **0.29.4** | scheduler 三连：组合模式 + 持久化 + 同名摘旧（本页） |
| 0.29.3 | 多实例通信 / MCP 两级化等（未单独立页；本机重启后与本批一并生效） |
| 0.28.1 | `Optional[X]` 工具参数注解修复（见 [v0.28.1](v0.28.1.md)） |
| 0.28.0 | 压缩阶梯三改 + 投影模拟器（见 [v0.28.0](v0.28.0.md)） |

## 相关页面

- [定时/到点任务调度](../features/background-scheduler.md) — 三件内容的机制详述
- [知识库导航](../home.md) — 版本状态总览
